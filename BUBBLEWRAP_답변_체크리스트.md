# Bubblewrap 초기화 답변 체크리스트

**터미널에서 다음 질문들에 순서대로 답변하세요:**

---

## ✅ 이미 완료
- [x] Android SDK 설치: `Y`

---

## 🔄 다음 질문들

### 2. Android SDK 라이선스 동의
```
? Do you agree to the Android SDK terms and conditions? (Y/n)
```
**답변:** `Y` (Enter)

### 3. App name
```
? Application name:
```
**답변:** `NexSupply` (Enter)

### 4. Package ID
```
? Package ID:
```
**답변:** `com.nexsupply.app` (Enter)

### 5. Host URL
```
? Host URL:
```
**답변:** `app.nexsupply.app` (Enter)

### 6. Create signing key
```
? Do you want to create a new signing key? (Y/n)
```
**답변:** `Y` (Enter)

### 7. Key password
```
? Key password:
```
**답변:** 원하는 비밀번호 입력 (예: `nexsupply2024`) (Enter)

### 8. Confirm password
```
? Confirm password:
```
**답변:** 같은 비밀번호 다시 입력 (Enter)

---

## 🎯 빠른 참조

**모든 답변 요약:**
1. Android SDK 설치: `Y` ✅
2. 라이선스 동의: `Y`
3. App name: `NexSupply`
4. Package ID: `com.nexsupply.app`
5. Host URL: `app.nexsupply.app`
6. Create signing key: `Y`
7. Key password: (원하는 비밀번호)
8. Confirm password: (같은 비밀번호)

---

**터미널에서 위 질문들에 답변해주세요!**



